## 2026-05-27 - Tedd.MortonEncoding Test Expansion
**Observation:** Discovered partial branch coverage resulting from the `#if INTRINSIC` implementation using System.Runtime.Intrinsics.X86.Bmi2. The test suite already included combinatorial `Parallel.For` LUT checks for `Encode`/`Decode` in both 2D and 3D, but still benefited from explicit boundary-condition coverage for related paths such as `SplitXY` and `SplitXYZ`. Hardware specific optimizations make achieving true 100% path coverage difficult in a single pass without isolating test configurations.
**Strategic Action:** Added bounded representative Xunit `[Theory]` / `[InlineData]` cases covering maximum buffers (16-bit for 2D, 10-bit for 3D), out-of-bounds indices, and extreme patterns to complement the existing combinatorial LUT coverage rather than replace or exhaust it. Acknowledged environment limitations on hardware intrinsics branches while improving confidence in deterministic behavior on software fallback routines.
