## 2025-02-18 - Architectural Documentation Synchronization

**Observation:** The README.md file contains outdated .NET framework references (e.g., .Net Core 3, .Net 4, .Net core 1/2) and does not clearly reflect the repository's currently supported target frameworks. The examples are correct but somewhat simplistic. Furthermore, the library now includes `SplitXY` and `SplitXYZ` functions, and a 3D encode/decode (`Encode(x,y,z)`, `Decode(morton, out x, out y, out z)`) functionality, which are completely undocumented in the README. The hardware acceleration explanation also lacks mention of .NET intrinsics support such as `X86.Bmi2`.

**Strategic Action:** Update README.md to describe the library's current structure and capabilities, including 3D encoding/decoding and the utility of `SplitXY`/`SplitXYZ`, and refresh the hardware acceleration section to mention supported intrinsic-based acceleration where applicable. Replace legacy testing syntax examples with a clearer demonstration structure. Ensure any framework references match the repository's actual supported TFMs rather than introducing newer-version guidance.
